$(document).ajaxStart(function(){  $('#loading').show();}).ajaxStop(function(){  $('#loading').hide();});
$('#hotel_city_id').html("#{escape_javascript( options_from_collection_for_select(@city, :id, :title_fa))}");
